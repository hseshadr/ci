# privacy-core — .github/workflows/ci.yml
# Single-package TS repo at root — the cleanest fit for frontend-gate.yml (no model
# weights, no injected caching). gate = biome -> tsc -> vitest -> test:e2e -> build,
# so it needs a Playwright browser (installed + cached by the template).
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: read
jobs:
  gate:
    uses: hseshadr/ci/.github/workflows/frontend-gate.yml@36bf999acd0617135497b62605e19bed29ee1b94 # ci-v2.0.0
    with:
      install-args: "--frozen-lockfile --config.dangerously-allow-all-builds=true"
      install-playwright: true
  gitleaks:
    uses: hseshadr/ci/.github/workflows/secret-scan.yml@36bf999acd0617135497b62605e19bed29ee1b94 # ci-v2.0.0
