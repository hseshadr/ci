# Known consumer drift — controls a consumer repository hand-rolls today that
# hseshadr/ci already publishes.
#
# FORMAT (one entry per line):
#
#     <repo>/<workflow-file>/<category>|<reason>
#
#   `#` starts a comment. Blank lines are ignored. The reason is MANDATORY —
#   tests/consumer-drift.sh exits 2 on an entry without one. An allowlist whose
#   entries carry no reason is a mute suppression list nobody can ever safely
#   delete from, which is how five hand-rolled Pages deploys survived long
#   enough for one of them to grow a fork-PR deploy vulnerability.
#
# THIS FILE IS A CONVERGENCE BACKLOG, NOT AN EXEMPTION LIST. Every entry is
# something to delete. Deleting one is free: the detector reports an allowlisted
# entry that no longer drifts as stale and warns — it never fails.
#
# Populated 2026-07-26 from the first live sweep: 30 hand-rolled controls across
# all 7 consumers.
#
# Corrected 2026-07-31 to 29. One entry (almamesh/deploy.yml/secret-scan) was a
# CLASSIFIER FALSE POSITIVE — the word "gitleaks" appears there only inside a
# shell comment — and was deleted along with the bug that produced it. Four
# reasons asserted something untrue and were rewritten; a reason nobody can trust
# is worse than no allowlist, because it is what people read before deciding not
# to converge. Every reason below has been checked against the live repo.
#
# 30 again on 2026-08-02, and this time the extra one IS new drift:
# aml-filter/ci.yml/secret-scan. Until then this header said "nothing here is new
# drift", which was true for exactly seven days and is the kind of sentence that
# quietly stops being true. It is not claimed any more — read each entry instead.
# The count went 30 (07-26, one of them bogus) -> 29 (07-31, bogus one deleted)
# -> 30 (08-02, one genuinely new). Only the last step is real drift.
#
# Still 30 entries on 2026-08-15: the converged edge-proc/ci.yml/secret-scan row
# was removed, and edge-proc/publish.yml/secret-scan now records a shared control
# gap rather than pretending the release preflight is ordinary CI. Its tag-triggered
# action range may be empty, so the release also runs one explicit full-history CLI
# sweep. Delete the entry when the shared control can prove the same tag behavior.
#
# A NEW ENTRY IS A CONFESSION, NOT A DECISION. It records that a control got
# hand-rolled and says what is being done about it. If you are adding one, the
# reason has to survive a stranger asking "why didn't you just adopt the brick?" —
# and if the honest answer is that the brick did not fit, then the finding is
# about hseshadr/ci and the fix belongs there, not in this file.

# --- pages-deploy (4) --------------------------------------------------------
# The control this detector was built for. Each of these builds a dist dir and
# pushes it to Cloudflare Pages with its own wrangler invocation, while
# cloudflare-pages-deploy.yml + the pages-deploy-dist composite sit unused.
aml-filter/deploy.yml/pages-deploy|Converged caller already drafted at examples/aml-filter/deploy.yml in this repo; the live repo has not adopted it. Adoption is the fix, not an exemption.
edge-reco/deploy.yml/pages-deploy|Converged caller already drafted at examples/edge-reco/deploy.yml; the live repo still runs `npx wrangler@4.103.0 pages deploy frontend/app/dist` inline.
almamesh/deploy.yml/pages-deploy|Bespoke: builds the Pyodide runtime + models and re-verifies deployed source identity after upload. No examples/almamesh/deploy.yml exists yet — converge after aml-filter and edge-reco land.
aml-filter/publish-watchlist.yml/pages-deploy|Not a site deploy: signs a watchlist bundle with a decoded key, then pushes it to the same Pages project. Needs a signing-aware pre-deploy hook before it can call the reusable workflow.

# --- secret-scan (8) ---------------------------------------------------------
# gitleaks runs as a step inside an existing job rather than as a job calling
# secret-scan.yml. Converging means splitting a job out — mechanical, but it
# touches every consumer, so it is batched deliberately rather than forgotten.
almamesh/test.yml/secret-scan|Uses gitleaks/gitleaks-action directly instead of calling secret-scan.yml.
aml-filter/security-audit.yml/secret-scan|Uses gitleaks/gitleaks-action directly; examples/aml-filter/security-audit.yml already shows the converged caller.
# THE ONLY ENTRY HERE THAT IS NOT FROM THE ORIGINAL 2026-07-26 SWEEP, and the one
# most likely to be read as this file doing the thing it says it never does.
# Added 2026-08-02 as a POINTER TO AN OPEN PR, not as an exemption: the fix is
# already written and cannot be merged from the same change that records it.
# Delete this line when that PR lands — the detector reports a stale entry as a
# warning, never a failure, so deleting it early costs nothing.
aml-filter/ci.yml/secret-scan|NEW on 2026-08-02, and being converged, not exempted — see aml-filter#93. Origin: aml-filter#89 (2026-08-01) closed a real hole (gitleaks ran only in the weekly security-audit.yml sweep, never on a PR, so a secret could merge and sit in public history for up to seven days) and closed it by inlining gitleaks/gitleaks-action at the same pinned SHA secret-scan.yml already uses. Three causes on OUR side, all addressed: examples/aml-filter/ci.yml carried no secret-scan job, so the worked example for the exact file being edited had nothing to copy; nothing warned that adopting renames the check run to "Secret scan / gitleaks", which breaks a required status check named "gitleaks"; and our ci-vX.Y.Z release scheme fails a consumer pin-comment guard that expects ^v\d, which is what actually reddened aml-filter#93 on its first run. DELETE THIS ENTRY when aml-filter#93 merges.
assay/ci.yml/secret-scan|Uses gitleaks/gitleaks-action directly; assay has no converged examples/assay/ci.yml yet.
edge-proc/publish.yml/secret-scan|Tag-release preflight runs the canonical gitleaks CLI over --all because gitleaks-action derives its range from the tag-push event and may inspect zero commits; keep until a shared control can prove full-history scanning on tag pushes.
edge-reco/ci.yml/secret-scan|Uses gitleaks/gitleaks-action directly; examples/edge-reco/ci.yml already shows the converged caller.
edgeproc-core/ci.yml/secret-scan|Uses gitleaks/gitleaks-action directly; examples/edgeproc-core/ci.yml already shows the converged caller.
privacy-core/ci.yml/secret-scan|Uses gitleaks/gitleaks-action directly; examples/privacy-core/ci.yml already shows the converged caller.

# --- dependency-audit (7) ----------------------------------------------------
# Every consumer has its own security-audit.yml predating security-audit.yml
# being callable, and each encodes a repo-specific audit floor, so convergence
# is per-repo work rather than one sweep.
almamesh/security-audit.yml/dependency-audit|Inline pip-audit; examples/almamesh/security-audit.yml already shows the converged caller.
aml-filter/security-audit.yml/dependency-audit|Inline pnpm audit; examples/aml-filter/security-audit.yml already shows the converged caller.
assay/security-audit.yml/dependency-audit|Inline pip-audit; assay has no converged examples/assay/security-audit.yml yet.
edge-proc/security-audit.yml/dependency-audit|Inline pip-audit; examples/edge-proc/security-audit.yml already shows the converged caller.
edge-reco/security-audit.yml/dependency-audit|Inline pnpm audit plus pip-audit via `uv run poe audit`; examples/edge-reco/security-audit.yml already shows the converged caller.
edgeproc-core/security-audit.yml/dependency-audit|Inline pip-audit; examples/edgeproc-core/security-audit.yml already shows the converged caller.
privacy-core/security-audit.yml/dependency-audit|Inline pnpm audit; examples/privacy-core/security-audit.yml already shows the converged caller.

# --- python-gate (5) ---------------------------------------------------------
almamesh/test.yml/python-gate|Runs `uv run poe` inline. NOTE: an earlier reason here claimed the gate "spans Python + browser and has never been split" — that was false. test.yml already has five jobs (secret-scan, backend, frontend, exit-gate, e2e-smoke) and `backend` is an exact python-gate.yml fit (`uv sync --locked --extra dev` then `uv run poe gate`, working-directory backend). This is ready to converge; keep the caller job named "Backend Tests" so the required status check survives.
assay/ci.yml/python-gate|Runs `uv run poe` inline; assay has no converged examples/assay/ci.yml yet.
edge-proc/ci.yml/python-gate|Runs `uv run poe` inline; examples/edge-proc/ci.yml already shows the python-gate.yml caller.
edge-reco/ci.yml/python-gate|Runs `uv run poe` inline; examples/edge-reco/ci.yml already shows the python-gate.yml caller.
edgeproc-core/ci.yml/python-gate|Runs `uv run poe` inline; examples/edgeproc-core/ci.yml already shows the python-gate.yml caller.

# --- frontend-gate (6) -------------------------------------------------------
# Note the bar here: using the setup-playwright composite counts as adoption,
# because for browser installs the composite IS the shared unit. Everything
# below installs browsers or runs the gate with neither the composite nor
# frontend-gate.yml.
almamesh/nightly-e2e.yml/frontend-gate|Installs Playwright browsers by hand in the nightly e2e run; should use the setup-playwright composite at minimum.
almamesh/test.yml/frontend-gate|Installs Playwright browsers by hand inside the combined test workflow.
aml-filter/ci.yml/frontend-gate|Runs `pnpm gate` inline; examples/aml-filter/ci.yml already shows the converged shape (setup-pnpm + setup-playwright composites).
assay/ci.yml/frontend-gate|Runs `pnpm gate` inline; assay has no converged examples/assay/ci.yml yet.
edge-reco/ci.yml/frontend-gate|Runs `pnpm gate` inline; examples/edge-reco/ci.yml already shows the converged shape (setup-pnpm + restore-model-cache + setup-playwright).
privacy-core/ci.yml/frontend-gate|Runs `pnpm gate` inline; examples/privacy-core/ci.yml already shows the frontend-gate.yml caller.
